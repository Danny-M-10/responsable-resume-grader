# Core dependencies (required)
PyPDF2>=3.0.0
python-docx>=0.8.11
reportlab>=4.0.0
matplotlib>=3.7.0
streamlit>=1.28.0

# OCR dependencies (optional - for scanned PDFs and images)
Pillow>=10.0.0
pytesseract>=0.3.10
pdf2image>=1.16.3

# Note: Also requires Tesseract OCR engine to be installed on system:
# macOS: brew install tesseract
# Ubuntu: sudo apt-get install tesseract-ocr
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
